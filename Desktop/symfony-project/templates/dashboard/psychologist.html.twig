# Replace the file with the merged version above
git add templates/dashboard/psychologist.html.twig

# If you have both admin and psychologist files staged
git commit -m "Resolve merge conflicts in psychologist dashboard - keep enhanced HEAD version with crisis alerts and Face ID"

# Push the changes
git push